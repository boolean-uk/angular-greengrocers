export interface Item {
  id: string;
  name: string;
  type: string;
  price: number;
}

export interface Product {
  id: string;
  quantity: number;
  name: string;
  type: string;
  price: number;
}
